name: shared
description: Shared UI widgets and components package.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.13.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # Local Packages
  config:
    path: ../config
  core:
    path: ../core

  # Routing
  go_router: ^17.1.0

  # UI Components
  shimmer: ^3.0.0

  # Utilities
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
